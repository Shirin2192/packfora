RewriteEngine On
RewriteBase /packfora/packfora/

# Skip if file or directory exists
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route all slugs to a single handler
RewriteRule ^([a-zA-Z0-9\-]+)$ slug-router.php?slug=$1 [QSA,L]
